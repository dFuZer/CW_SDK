namespace ExampleMod;

public class MyPluginInfo
{
    // Replace with your own plugin guid. Make sure it is unique.
    // Also make sure to update the version and plugin name.
    // Don't modify the vanilla compatible value unless you are sure about what you are doing.
    // That value should only be set to true to host-only or client-only mods.
    public const string PLUGIN_GUID = "example.mod.guid";
    public const string PLUGIN_NAME = "EXAMPLE MOD";
    public const string PLUGIN_VERSION = "0.0.0";
    public const bool VANILLA_COMPATIBLE = false;
}
